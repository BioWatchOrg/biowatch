[project]
name = "biowatch-backend"
version = "0.1.0"
description = "BioWatch backend"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "geoalchemy2>=0.20.0",
    "h3>=4.5.0",
    "httpx>=0.28.1",
    "mcp>=1.27.2",
    "pandas>=3.0.3",
    "psycopg[binary]>=3.3.4",
    "pyarrow>=24.0.0",
    "python-dotenv>=1.2.2",
    "shapely>=2.1.2",
    "sqlalchemy>=2.0.51",
]

[project.scripts]
biowatch-jobs = "jobs.cli:main"

[dependency-groups]
dev = [
    "pytest>=8",
    "ruff>=0.6",
    "mypy>=1.11",
    "types-shapely>=2.1.0.20260630",
    "pandas-stubs>=3.0.3.260530",
    "pytest-cov>=7.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = [
    "apps/api",
    "apps/jobs",
    "packages/clients",
    "packages/core",
    "packages/geo",
    "packages/ml",
    "packages/scoring",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.pytest.ini_options]
testpaths = ["apps", "packages", "tests"]

[[tool.mypy.overrides]]
module = "h3.*"
ignore_missing_imports = true
